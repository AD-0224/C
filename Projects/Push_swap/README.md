Push Swap in C | Tri optimisé de piles en C

A C program that sorts a stack of integers using a limited set of operations (sa, sb, pa, pb, ra, rb, rra, rrb).
Un programme en C qui trie une pile d’entiers en utilisant un nombre limité d’opérations (sa, sb, pa, pb, ra, rb, rra, rrb).

---

🧠 Features | Fonctionnalités

✅ Sorts a stack of integers with a minimal number of operations
✅ Supports both small and large stacks
✅ Implements Radix Sort for large stacks
✅ Handles invalid input (duplicates, non-integer values)
✅ Clean and readable C99 code

---

📁 Structure

Push_swap/
├── include/
│ └── PUSH_SWAP.h
├── src/
│ ├── main.c
│ ├── push_swap.c
│ ├── operations.c
│ └── utils.c
├── build/ # (generated by Makefile)
├── bin/ # (generated by Makefile)
├── Makefile
└── README.md

---

⚙️ Compilation & Execution | Compilation & exécution

With Make:

make        # compile the push_swap program
make run    # run push_swap
make re     # clean and rebuild
make debug  # compile with debug symbols and launch GDB


Run directly:

./bin/push_swap_app 3 2 5 1 4

---

🔍 Function Overview | Présentation des fonctions
Function Name	Description (EN)	Description (FR)
create_stack	Initializes a stack	Initialise une pile
push	Pushes element to a stack	Ajoute un élément à la pile
pop	Removes element from a stack	Supprime un élément de la pile
swap	Swaps the first two elements	Échange les deux premiers éléments
rotate	Rotates stack upwards	Fait tourner la pile vers le haut
reverse_rotate	Rotates stack downwards	Fait tourner la pile vers le bas
radix_sort	Sorts the stack using Radix Sort	Trie la pile avec Radix Sort
is_sorted	Checks if the stack is sorted	Vérifie si la pile est triée
execute_operation	Executes a single instruction (sa, pb, etc.)	Exécute une instruction sur la pile
🧪 Sample Output | Exemple de sortie
./bin/push_swap_app 3 2 5 1 4
ra
ra
ra
pb
ra
ra
pb
ra
sa
pa
pa


Each line represents an operation to sort the stack.
Chaque ligne correspond à une opération pour trier la pile.

---

🧪 Testing & Debugging | Tests et débogage

Debugging with Makefile | Débogage avec Makefile

make debug


Adds debug symbols (-g)

Cleans and recompiles

Launches GDB for step-by-step debugging

Example GDB commands:

run                 # Start program
break main          # Breakpoint at main
next                # Step to next line
print stack_a       # Inspect stack contents
continue            # Continue execution
quit                # Exit GDB


Memory checking with Valgrind | Vérification mémoire avec Valgrind

valgrind --leak-check=full ./bin/push_swap_app 3 2 5 1 4


Detects memory leaks and invalid accesses

Ensures the program exits cleanly → Valgrind should report 0 leaks

--- 

⚡ Limitations / Future improvements | Limitations / Améliorations futures

Optimizations could further reduce the number of instructions for large stacks

No visual output of stack state (could add a graphical display)

Currently supports only integers

---

🛡️ License | Licence

This project is licensed under the MIT License — feel free to use, modify, and share.
Ce projet est sous licence MIT — libre à vous de l’utiliser, le modifier et le partager.